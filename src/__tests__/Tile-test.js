// jest.unmock('../sharedComponents/tile1x/Tilex');


// import React  from 'react';
// import { shallow } from 'enzyme';
// import Tilex from '../sharedComponents/tile1x/Tilex';

// describe('Tilex  Tests', () => {
	
//     const wrapper = shallow(<Tilex />);
//     console.log(wrapper)
// 	it('Should contain 4 div elements',()=>{
//   		expect(wrapper.find('.gorTile')).to.have.length(1);
// 	});

// 	it('Should contain 1 div with class "up-tile"',()=>{
// 		expect(wrapper.find(div.gorUp-tile)).to.have.length(1);
// 	});

// 	it('Should contain "Items in stock" in heading',()=>{
// 		expect(wrapper.find('.gorHeading').text()).to.equal('Items in stock');
// 	});

// 	it('Cart should be of type image',()=>{
// 		expect(wrapper.find('cart').type()).to.equal('img');

// 	});

// 	it('Expect 2 images',()=>{
// 		expect(wrapper.find('img').length).to.equal(2);

// 	});

// 	it('Expect no buttons',()=>{
// 		expect(wrapper.find('button').length).to.equal(0);
// 	});

// });

